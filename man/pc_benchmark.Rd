% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pc_api.R
\name{pc_benchmark}
\alias{pc_benchmark}
\title{Benchmark Chunked PubChem Workflows}
\usage{
pc_benchmark(
  ids,
  fn,
  chunk_sizes = c(25, 50, 100),
  parallel_options = c(FALSE),
  workers = NULL,
  ...
)
}
\arguments{
\item{ids}{Identifier vector.}

\item{fn}{Function applied by `pc_batch()`.}

\item{chunk_sizes}{Integer vector of chunk sizes.}

\item{parallel_options}{Logical vector controlling parallel toggle.}

\item{workers}{Number of workers used when parallel is enabled.}

\item{...}{Additional arguments passed to `fn`.}
}
\value{
A tibble with runtime and success metrics for each benchmark scenario.
}
\description{
Evaluates `pc_batch()` execution under multiple chunk-size and parallel
settings and returns runtime and failure metrics.
}
\details{
This benchmark is intended for tuning operational parameters before running
large production queries.
}
\examples{
bm <- pc_benchmark(
  ids = 1:20,
  fn = function(chunk_ids, ...) sum(chunk_ids),
  chunk_sizes = c(5, 10),
  parallel_options = FALSE
)
nrow(bm)
}
