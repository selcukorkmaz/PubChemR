% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pc_phase4.R
\name{pc_sdq_bioactivity}
\alias{pc_sdq_bioactivity}
\title{Retrieve Full Biological Test Results from PubChem SDQ}
\usage{
pc_sdq_bioactivity(
  identifier,
  namespace = "cid",
  collection = "bioactivity",
  limit = 10000000L,
  order = "activity,asc",
  rate_limit = TRUE,
  cache = FALSE,
  cache_dir = NULL,
  cache_ttl = NULL,
  force_refresh = FALSE
)
}
\arguments{
\item{identifier}{A single compound identifier (CID, name, or InChIKey
depending on \code{namespace}).}

\item{namespace}{Character. The namespace for \code{identifier}. Default
\code{"cid"}. If not \code{"cid"}, the identifier is first resolved to a
CID via \code{\link{pc_request}}.}

\item{collection}{Character. SDQ collection to query. Default
\code{"bioactivity"}.}

\item{limit}{Integer. Maximum number of rows to return. Default
\code{10000000L}.}

\item{order}{Character. Column and direction for sorting results. Default
\code{"activity,asc"}.}

\item{rate_limit}{Logical or numeric. If \code{TRUE} (default), applies
the global rate limit from \code{\link{pc_config}}. If numeric, uses that
value as requests per second.}

\item{cache}{Logical. If \code{TRUE}, cache results to disk. Default
\code{FALSE}.}

\item{cache_dir}{Character. Directory for disk cache. Defaults to the value
from \code{\link{pc_config}}.}

\item{cache_ttl}{Numeric. Cache time-to-live in seconds. Defaults to the
value from \code{\link{pc_config}}.}

\item{force_refresh}{Logical. If \code{TRUE}, bypass any cached result.
Default \code{FALSE}.}
}
\value{
A tibble of class \code{PubChemTable} containing the full
  bioactivity results.
}
\description{
Queries the PubChem SDQ (Structured Data Query) agent to retrieve the full
biological test results table for a compound. Uses the \code{download} query
mode to return all available columns for each record. The number and names
of columns vary by compound depending on available data (e.g. baid, aid,
sid, cid, activityid, aidtypeid, aidname, targetname, cmpdname, acvalue,
geneid, etc.).
}
\details{
When \code{namespace != "cid"}, the identifier is first resolved to CID via
\code{\link{pc_request}} before querying SDQ. Returned columns depend on
source availability for the requested compound.
}
\examples{
names(formals(pc_sdq_bioactivity))

\dontrun{
  # Retrieve bioactivity data for aspirin (CID 2244)
  bio <- pc_sdq_bioactivity(2244)
  head(bio)
}

}
