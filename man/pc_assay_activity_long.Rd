% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pc_phase4.R
\name{pc_assay_activity_long}
\alias{pc_assay_activity_long}
\title{Convert PubChem Assay Summary to Long Activity Table}
\usage{
pc_assay_activity_long(
  identifier = NULL,
  namespace = "cid",
  x = NULL,
  chunk_size = NULL,
  unique_rows = TRUE,
  add_outcome_value = TRUE,
  outcome_map = NULL,
  strict_outcome = FALSE,
  unknown_outcome = NA_real_,
  ...
)
}
\arguments{
\item{identifier}{Identifier vector used when `x` is `NULL`.}

\item{namespace}{Namespace for `identifier` when requesting from PubChem.}

\item{x}{Optional source object. One of:
- `PubChemResult` from `pc_request(...)`
- raw parsed payload list containing `Table/Columns/Row`}

\item{chunk_size}{Optional chunk size for large identifier vectors.}

\item{unique_rows}{Logical. Remove duplicate rows after normalization.}

\item{add_outcome_value}{Logical. If `TRUE`, adds a numeric
`ActivityOutcomeValue` column when `ActivityOutcome` exists.}

\item{outcome_map}{Optional named mapping passed to
`pc_activity_outcome_map()`.}

\item{strict_outcome}{Logical. If `TRUE`, unknown outcome labels error.}

\item{unknown_outcome}{Numeric fallback for unknown labels when
`strict_outcome = FALSE`.}

\item{...}{Additional arguments forwarded to `pc_request()` when `x` is `NULL`.}
}
\value{
A tibble with normalized fields including `CID`, `AID`,
  and `ActivityOutcome` when available.
}
\description{
Builds a normalized long-form table from the PubChem
`compound/*/assaysummary/JSON` payload, suitable for
`pc_activity_matrix()`.
}
