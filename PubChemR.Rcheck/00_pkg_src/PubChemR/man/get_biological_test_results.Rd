% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_biological_test_results.R
\name{get_biological_test_results}
\alias{get_biological_test_results}
\title{Retrieve "Biological Test Results" Section from PubChem Contents}
\usage{
get_biological_test_results(
  identifier,
  domain = "compound",
  heading = "Biological Test Results",
  .match_type = c("match", "contain"),
  .all = FALSE,
  .verbose = FALSE,
  ...
)
}
\arguments{
\item{identifier}{A single identifier for a PUG View request.}

\item{domain}{A domain value accepted by \code{\link{get_pug_view}}.}

\item{heading}{Section heading to search for. Defaults to
\code{"Biological Test Results"}.}

\item{.match_type}{Matching strategy for \code{heading}; one of
\code{"match"} (exact, case-insensitive) or \code{"contain"}.}

\item{.all}{Logical. If \code{TRUE}, returns all matching sections as a
\code{PugViewSectionList} object. If \code{FALSE}, returns the first
matching section as a \code{PugViewSection} object.}

\item{.verbose}{Logical. If \code{TRUE}, prints the returned section object
and returns it invisibly.}

\item{...}{Additional arguments passed to \code{\link{get_pug_view}}.}
}
\value{
A \code{PugViewSection} object (default) or \code{PugViewSectionList}
when \code{.all = TRUE}. If no section is found, a failed
\code{PugViewSection} object is returned with error details.
}
\description{
This helper fetches a PUG View record and extracts section(s) with heading
\code{"Biological Test Results"} (or a custom heading) from the PubChem
\code{CONTENTS} structure.
}
\examples{
\donttest{
bio <- get_biological_test_results(identifier = "2244", domain = "compound")
bio

# Return all matching sections (if multiple are present)
bio_all <- get_biological_test_results(
  identifier = "2244",
  domain = "compound",
  .all = TRUE
)
bio_all
}
}
